rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // match /{document=**} {
    //   allow read, write: if
    //       request.time < timestamp.date(2021, 1, 28);
    // }
    
    
    //match docs in guides collection
    match /guides/{guideId} {
    	allow read, write: if request.auth.uid != null;
    }
  }
}